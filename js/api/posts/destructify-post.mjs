/* function destructifyPost(post) {
   const { title, body, tags, media, created, updated, author, comments, reactions, _count } = post;

   const { name, email, avatar, banner } = author;

   const { commentsCount, reactionsCount } = _count;

   tags.forEach((postTags) => {});

   comments.forEach((comment) => {});

   reactions.forEach((postReactions) => {});

   const singlePost = {
      media: media,
      title: title,
      body: body,
      tags: tags,
      created: created,
      updated: updated,
      author: author,
      comments: comments,
      reactions: reactions,
      count: _count,
   };

   console.log(post.reactions);
}
 */
